spring:
  data:
    mongodb:
      uri: mongodb://admin:password123@mongodb:27017/dnf_insight?authSource=admin

    redis:
      host: redis
      port: 6379

logging:
  level:
    com.dnf.insight: DEBUG
    org.springframework.web: DEBUG
    org.springframework.data.mongodb: DEBUG
